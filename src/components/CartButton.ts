import { Link } from 'react-router-dom'

import styled from 'styled-components'

export const BaseCartButton = styled(Link)`
	padding: 8px;
	position: relative;
	border: none;
	border-radius: 6px;
	line-height: 0;
	transition: all 0.2s ease-out;
`
